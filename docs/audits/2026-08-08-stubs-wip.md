# Stub / WIP Audit — 2026-08-08

Status: `PARTIAL_ALIVE`

## Admitted subject

- Repository: `seanchatmangpt/anti-llm-cheat-lsp`
- Base: `main`
- Observed tree SHA: `cc950786832e526f236caacfcf0578c898bc5178`

## Confirmed findings

### WIP-001 — LSIF 0.6 example evidence remains entirely OPEN

`docs/COVERAGE_LSP318_LSIF06.md` says all 38 LSIF elements are modeled, but the example carries **zero LSIF transcripts/receipts**, so every element's example-coverage status remains `OPEN` and cannot be `ADMITTED`.

**Classification:** `INCOMPLETE_WIP / EVIDENCE_COVERAGE`

**Recommended closure:** add dependency-closed LSIF fixtures/transcripts plus receipts for the modeled element graph and make the coverage matrix derive `ADMITTED` only from observed transcript + receipt evidence.

### WIP-002 — Full verification remains CANDIDATE and depends on sibling checkouts

The same coverage document says workspace compilation requires sibling checkouts `../lsp-types-max`, `../wasm4pm`, and `../wasm4pm-compat`, and status remains `CANDIDATE` until those siblings are present and the full test run is admitted.

**Classification:** `INCOMPLETE_WIP / DEPENDENCY_CLOSURE`

**Recommended closure:** make the verifier dependency-closed through pinned workspace dependencies, a reproducible capsule, or an explicit bootstrap/materialization command, then execute the full suite against the exact admitted source.

### WIP-003 — Canonical admissibility report contains machine-local evidence links

`admissibility_report.md` cites evidence using `file:///Users/sac/anti-llm-cheat-lsp/...` paths.

**Classification:** `INCOMPLETE_WIP / REPLAY_POINTER`

Those pointers cannot be replayed from a fresh checkout or by another verifier.

**Recommended closure:** replace machine-local file URLs with repository-relative/content-addressed evidence paths and bind them to exact source/receipt identities.

## Search limitation

Marker-oriented GitHub search returned upstream HTTP 502. The exact tree and the repository's own coverage/admissibility documents directly establish these findings.

## Standing

`PARTIAL_ALIVE`: explicit OPEN/CANDIDATE states and non-replayable evidence pointers were observed; the full workspace suite was not executed in this connector-only audit.
